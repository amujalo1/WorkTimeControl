#ifndef KONEKCIJA_H
#define KONEKCIJA_H

#endif // KONEKCIJA_H

#include <QMessageBox>
#include <QSqlDatabase>
#include <QSqlError>
#include <QSqlQuery>
#include <QMessageBox>
#include <QSqlTableModel>
#include <QtXml>
#include <QDebug>
#include <QDomDocument>
#include <QSqlQuery>
#include <QSqlError>
#include <QSqlRecord>
#include <QString>
#include <QMainWindow>
#include <QSqlQueryModel>
#include <QSqlTableModel>

/*static bool createConnection(QString adresa)
{
   QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE");
   db.setDatabaseName(adresa);
   if (db.open()) {return true;}
   else
   QMessageBox::critical(nullptr, QObject::tr("Baza ne može biti otvorena"),
                      QObject::tr("Konekcija sa bazom nije uspjela.\n"
                                 "Kliknite na Cancel za izlaz."), QMessageBox::Cancel);

    return false;

   }
*/
